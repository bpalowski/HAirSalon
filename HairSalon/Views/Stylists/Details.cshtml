@if (@Model["selectedClients"].Count != 0)
{
<h2>Clients:</h2>
  <ul>
    @foreach(var client in Model["selectedClients"])
    {
    <li>@client.GetName()</li>
    }
  </ul>
}
<p><a href='/'>home</a></p>
